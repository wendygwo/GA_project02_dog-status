<h1>Add a new dog and owner relationship</h1>

<%=form_for @relationship do |f|%>
	<p>Dog: <%=f.collection_select :dog_id, Dog.all, :id, :dog_name %></p>
	<p>Owner: <%=f.collection_select :owner_id, Owner.all, :id, :full_name %></p>

	<%=f.submit 'Add this new relationship' %>
<% end %>