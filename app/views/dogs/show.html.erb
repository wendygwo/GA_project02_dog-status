<div class='container-fluid'>

	<div class='row'>
		<div class='col-sm-2'></div>
		<div class='col-sm-8'>
			<h1> <%=@dog.dog_name %>'s info</h1>
		</div>
		<div class='col-sm-2'></div>
	</div>


	<p>Breed: <%=@dog.breed %></p>
	<p>Birthday: <%=@dog.birthday %></p>
	<p>Food low checkbox? <%=check_box_tag 'food_low' , 'yes', @dog.food_low_flag, disabled:true %></p>
	<p>Treats low checkbox? <%=check_box_tag 'treats_low' , 'yes', @dog.treats_low_flag, disabled:true %></p>
	<% if @dog.image_file_name != nil%>
		<%= image_tag @dog.image.url(:thumb) %>
	<% end %>

	<!--**********************************-->
	<!-- Start dog status section         -->
	<!--**********************************-->
	<h3>My daily status</h3>
	<h6>Last updated at <%= @dog_daily_status.updated_at %> </h6>

	<p>Today I've been:</p>
	<%= check_box_tag 'is_fed_breakfast', 'yes', @dog_daily_status.is_fed_breakfast, disabled:true %> Fed breakfast <br />
  <%= check_box_tag 'is_fed_dinner', 'yes',@dog_daily_status.is_fed_dinner, disabled:true %> Fed dinner <br />
  <%= check_box_tag 'is_walked', 'yes',@dog_daily_status.is_walked, disabled:true %> Walked <br />
  Given <%= @dog_daily_status.num_treats_given %> treats
  <br />
  Notes:
  <%= @dog_daily_status.notes %><br />



	<!--**********************************-->
	<!-- Start owners section             -->
	<!--**********************************-->
	<h3>My owners are</h3>

		<!-- Loop to show all owners for this dog -->
	<div class='row'>
		<div class='col-sm-1'></div>
		<div class='col-sm-10'>
			<table>	
				<thead>
					<th>Picture</th>
					<th>Name</th>
					<th colspan="3">Actions</th>
				</thead>
				<tbody>
					<% @owners.each do |owner| %>
						<!-- Find the current relationship record, to allow for relationship manipulations -->
						<% current_relationship = Relationship.where(owner_id: owner.id, dog_id: @dog.id).first %>
						<tr>
							<td><%= image_tag owner.image.url(:thumb)%></td>
							<td><%= owner.full_name%></td>
							<td><%= link_to "See owner details", owner_path(owner) %></td>

							<!-- This creates dynamic info about if the owner on this row is a dog admin and allows you to change that if logged in user is a dog admin -->
								<% if is_current_owner_site_admin || @is_dog_admin%>
									<td>Is 
										<% if !current_relationship.is_dog_admin %>
											not
										<% end %>
										a dog admin. <br />

										<!-- Give a link to edit dog admin status-->
										Click <%=link_to "here", edit_relationship_path(current_relationship) %> 	to edit this.
									</td>
								<% end %>
						</tr>
					<% end %>
				</tbody>
			</table>

		</div>
		<div class='col-sm-1'></div>
	</div> <!-- End row div -->

	<div class='row'>

	</div>

	<div class='row'>

	</div>
</div><!--  End fluid container div -->

<br />
<% if is_current_owner_site_admin || @is_dog_admin%>
	<%= link_to 'Add new owner(s) for this dog', new_relationship_path(dog_id: @dog.id) %> <br />
	<br />
	<!-- TO DO: need to delete all relationships linked to this dog, in addition to deleting the dog-->
	<%= link_to 'Delete this dog', :dog, method: :delete, data: {confirm: 'Are you sure you want to delete this dog?'} %> 
<% end %>


<br />
***======================================================================================================*** <br />
	<p>Parameters passed from dog controller, show method. Delete later!!!</p>
	<p>is_dog_admin: <%= @is_dog_admin %></p>
	<p>is_site_admin:	<%= is_current_owner_site_admin %></p>
	***======================================================================================================*** <br />
