<%= @greeting %> <%= @owner.first_name %>, <br>

<p>To reset your password, click the URL below:</p>

<p><%= link_to 'Reset password', edit_password_reset_url(@owner.password_reset_token) %></p> <!--This is the link to reset your password-->

<p>If the link above doesn't work, copy and paste the URL below into your address bar:</p>
<p><%= edit_password_reset_url(@owner.password_reset_token) %></p>
<p>If you did not request your password to be reset, just ignore this e-mail and your password will stay the same.</p>